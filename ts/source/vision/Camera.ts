
// camera for the game
export class Camera {


    // detect if room is on screen at all

    // detect if object is on camera

    // transform object absolute coordinates to relative coordinates

    // render the thing.
}

