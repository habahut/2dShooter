
// For now unused, if we need to improve hit detection for some stuff we can add this back in
export class Circle {
    radius: number;
    x: number;
    y: number;

    constructor(x: number, y: number, radius: number) {
        this.x = x;
        this.y = y;
        this.radius = radius;
    }
}

